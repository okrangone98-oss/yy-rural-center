# GAS Open Check

다음 파일들은 비어 있지 않습니다.

- `iumteo/docs/gas/yy-integrated-backend-v3.gs`
- `iumteo/docs/gas/yy-local-badge-sync.gs`
- `iumteo/docs/gas-deployment-guide.md`

확인된 파일 크기:

- `yy-integrated-backend-v3.gs`: `13365 bytes`
- `yy-local-badge-sync.gs`: `1274 bytes`
- `gas-deployment-guide.md`: `1764 bytes`

앞부분 예시:

```text
/**
 * Yangyang Iumteo Integrated Backend GAS v3
```

```text
/**
 * 양양이음터 로컬 배지 동기화
```

```text
# GAS Deployment Guide
```

VS Code에서 빈 화면처럼 보이면 아래를 먼저 확인합니다.

1. 탐색기에서 파일을 다시 클릭해 새 탭으로 엽니다.
2. 우상단의 `Open Preview` 상태가 아니라 일반 탭인지 확인합니다.
3. `Reopen Editor With...`에서 `Text Editor`로 다시 엽니다.
4. 파일 경로가 `iumteo/docs/...`인지 다시 확인합니다.
